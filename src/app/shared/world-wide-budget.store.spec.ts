import {
  describe,
  ddescribe,
  expect,
  iit,
  it
} from '@angular/core/testing';
import {WorldWideBudget} from './world-wide-budget.Store';

describe('WorldWideBudget', () => {
  it('should create an instance', () => {
    expect(new WorldWideBudget()).toBeTruthy();
  });
});
